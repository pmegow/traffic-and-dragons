# Fable UberAudit — whole-project capability review (2026-07-09, engine v1.233)

Three parallel deep reads run in one session: ① the full audit corpus (all `AUDIT_*.md` + `RAG_MEMORY.md` / `STORY_COMPILER.md` / `BLUEPRINT_EDITOR.md`), ② the core engine (`api.js` / `memory.js` / `state.js` / `game.js` / `globals.js`), ③ the UI & support layer (`ui.js` / `char-creation.js` / `storage-adapter.js` / helpers / data / tts / stt / compress / sw). Purpose: decide where frontier-model capability ("Fable" tier) pays off, and file everything else at its honest tier.

Format matches [TODO.md](TODO.md) (incl. the new **Tier** column — see its legend). Rows carry `UA` numbers; graduate a row into TODO.md when it's scheduled, and strike it here with a pointer. Items already tracked in TODO.md are NOT duplicated — where a finding *sharpens* an existing row, the row here says "enriches #N".

---

## ⛨ Standing policy — the drift-protection surface (user decree 2026-07-09)

> *"The best thing we've done so far is the drift management. Anything that in even the smallest way jeopardizes drift abatement is handled by Fable, the task is reviewed critically before any work is done, and checked thoroughly after completion."*

**The drift surface** (touching ANY of it triggers the policy): `applyMuts` write paths · all memory tiers (summarize / RAG / memoryTOC / futureEvents / alias-resolution / NPC merge) · `buildSysPrompt` canon-injection blocks, the stable/volatile cache split, and STYLE-at-end position · `cleanTxt` + the tag vocabulary + the STATE TAGS prompt docs · transcript integrity (`serializeWorldState`/`parseWorldState`, `logTranscript`) · quest/skeleton lifecycle teeth.

**The protocol:** ① **Tier = Fable**, no matter how small the task looks — drift-surface failure modes are SILENT (degraded canon, dead cache, fused NPCs), which is why "looks mechanical" is not a defense. ② **Critical pre-review before any code**: what does this touch, what silent failure could it cause, what's the test plan — presented to the user first. ③ **Thorough post-verification**: engine tests + stable-half byte-identity + prompt/mutation diff against real saved transcripts + live spot-check where warranted.

Rows below marked **⛨** are on the drift surface and carry the policy (their Tier is Fable regardless of intrinsic difficulty). Per-row **⚠ guard** notes state the specific hazard.

---

## The recommendation (session plan — reordered 2026-07-09: spine + lanes, playtest-gated blocks)

The reviews converge on one keystone: **the tag engine.** `applyMuts` is simultaneously the biggest reliability risk (233 lines, ~55 hand-written sequential parsers, ordering constraints documented only in comments), the biggest violation of the project's own dispatch-table principle, and the architectural blocker for server authority under the subscription model. The tag vocabulary lives in three places that drift silently (parsers · `cleanTxt` strip regexes · STATE TAGS prompt docs) — a live phantom tag (UA2) proves the failure mode.

**Structure: one serial SPINE for drift-surface work, overlap LANES for everything drift-neutral.** Cadence rule (user call 2026-07-09): spine blocks are **separated by playtests, not run back-to-back** — for drift-surface work the playtest IS the policy's "checked thoroughly after" (engine tests prove the injection is sent; only live turns prove the model obeys), and one-block-per-playtest keeps a regression's suspect list at exactly one block. Lanes don't need playtest gates (preview-verified, no drift contact) and may run continuously alongside.

**THE SPINE (serial, Fable ⛨, playtest after every block):**
1. **Session C — instrumentation & hardening FIRST** (moved from third: its tripwires protect everything after). ✅ **DONE (v1.234–v1.238 + server deploy, 2026-07-09)** — CAS turn guard + UA5 tripwire + UA3 transcript rescue + UA4 (phantom, pinned) + UA6 reorder + UA11 warns. → **Playtest 1: ✅ RUN (harness, same day)** — [AUDIT_playtest_v1238.md](AUDIT_playtest_v1238.md): C's guards validated live (0 tripwire warns across 4 summarize cycles, billing-corroborated single cache write), **UA35 money test PASSED**. Residual: CAS two-device confirm needs a human two-device session (harness is single-device). **Session A is UNBLOCKED** pending F1's small ⛨ fix (proposed in the audit). New findings F1 (slot softness) / F3 (quest-reopen double-pay) filed in the audit.
2. **Session A — UA1, the tag-table rebuild, shipped in SHADOW MODE**: design the descriptor schema together first (pre-review presented before any code); the table runs alongside the old parsers diffing mutations turn-by-turn, not yet authoritative. Auto-derives `cleanTxt` + the prompt docs; makes UA25/UA26/UA27 additive instead of surgery. → **The SOAK:** every real play session generates shadow-diff corpus (play is load-bearing, not optional); UA36's RAG A/B can ride here. **Cut over only after the diff log stays clean across sessions** → post-cutover playtest.
3. **Post-A additive batch** — UA25 (companion spells) + UA26 (multi-enemy combat) + UA28 (Haiku nudges, now safe behind UA5's tripwire). → **Playtest:** companion casting + multi-foe combat + Haiku window exercised live.
4. **Core Memory #40** (resolves UA14's write-only archive). → **Playtest:** defining-moments injection observed on a mature save.

**THE LANES (drift-neutral, run parallel to the spine, no playtest gate):**
- **Session B — UA33 `SERVER_ARCHITECTURE.md`** (Fable, pure design, zero code contact): survey the never-audited server first (UA32), then gateway / billing / auth / **state authority**. Slots naturally into A's soak window. Gates only the sync rewrite (UA19) and #44 — neither on the spine.
- **Session D — odds & ends** (Sonnet/Any): UA7, UA8, UA13 (with #30), UA20, UA22, UA34. Hand to cheaper sessions anytime.
- **The ui.js track** (Sonnet execution): UA17 decomposition (Fable draws the seam map — a 20-min byproduct of this review), UA21 dedup cluster, #22 via UA18's sink list. Courtesy rule: don't land the split the same week as A's cutover commit — keep the diffs reviewable.
- **The play checklist** (UA35–37) rides the spine's playtests — each between-block session retires validation debt while gating the next block.

**Hard sequencing rules (the only true serial constraints):** C before A · UA5 before UA28 · A before UA25/26/27 · B before any sync rewrite / #44.

**Do not spend Fable on** (and the policy does NOT bite): #30/UA13 usage pricing, UA17's decomposition *execution*, #22/UA18's mechanical escaping, UA20/UA22/UA34, and the money tests (those need play sessions, not horsepower). *(Note: UA25/26/28/29 were originally in this list — the drift policy moved them to Fable; their difficulty didn't change, their blast radius did.)*

**Watches (no work until a trigger fires):** UA15 (transcript growth → IndexedDB trigger), UA16 (skeleton self-correction → watch #23 live compliance first).

---

## Engine core findings (api.js · memory.js · state.js · game.js · globals.js)

| # | Task | Effort | Tier | Status |
|---|---|:---:|:---:|---|
| UA1 | ⛨ **Tag engine rebuild: `applyMuts` → descriptor-driven tag table.** Today: ~55 sequential hand-written parsers in one 233-line function (api.js:507–740), coupling character/npcs/combat/quests/skeleton/memory/map/npcGraph/companions + UI calls, with load-bearing ordering (LOCATION before LOCATION_DESC, stale-combat clear on move, `stampQuestCompletion` last) documented only in comments. The tag vocabulary is a **triple source of truth**: parsers + `cleanTxt` strip regexes (`_CT_TAGS`/`_CT_BARE`, api.js:417–418) + the STATE TAGS prompt block (api.js:229–274) — divergence is silent in both directions (unstripped tag shows to the player; unapplied tag vanishes). Build ONE registry per tag `{name, arity, docLine, handler, needsCombat?, npcKeyed?}`; derive the strip regex AND the prompt docs from it; shared helpers for the ~15 copy-pasted "resolve name → ensure memory entry → mutate" idioms; loud unknown-tag / tag-with-no-combat warnings (no-silent-failures, currently unenforced for combat — the v1.224 C1 no-op class). **Payoffs:** kills the phantom-tag class (UA2); makes multi-foe combat (UA26), `[COMPANION_SPELL_USED:]` (UA25), and every future tag an *entry* not surgery; makes the state machine testable in isolation and **portable to the server** — the prerequisite for subscription state authority (today UI calls are interleaved into mutation logic, api.js:662/676/738). **⚠ guard (drift):** ① `applyMuts` is the WRITE PATH of every memory tier the drift protections read — a parser regression poisons the data canon-injection re-reads forever after; diff-mode cutover is mandatory (both parsers on real saved transcripts until mutations are byte-identical). ② The derived STATE TAGS block must initially reproduce today's battle-tested text BYTE-IDENTICALLY (doc lines stored verbatim in the table); wording "improvements" are separate, A/B-tested changes — never bundled with the cutover. ③ The registry must be an ORDERED list, not object-key iteration — the ordering dependencies are themselves drift protections. | XL | Fable | ⚙ **SHADOW MODE SHIPPED (v1.241, overnight 2026-07-10; pre-review presented + approved 2026-07-09)** — `tag_table.js`: one ordered registry (~57 handler transcriptions, legacy order preserved); cleanTxt regexes + the STATE TAGS prompt block now DERIVE from it. Old parser stays authoritative; every response also runs the table on cloned state and diffs (loud + persisted `tnd_tagdiff_v1`; kill switch `TAG_SHADOW`). **Evidence:** stable half byte-identical to the pre-refactor golden (21,437 chars); strip regex hash-identical to the old literal; 270 engine tests green with ZERO shadow diffs incl. a full-vocabulary battery; the v1.238 corpus (40 raw Sonnet responses) replayed via dev/diff-replay.js with ZERO diffs and an end state matching the live run exactly; a fresh 18-turn combat/companion soak (dev/corpus_tagsoak_v1241.json — full combat lifecycle, party join + async sheeting, REST, XP mirror, 29 tag types) ran live with 19/19 clean parity runs. Coverage guards kill the phantom class; UA2's ENEMY_SURRENDERS formalized in `TAG_NO_HANDLER` pending its decision; UA27's null-combat warn + UA9/UA10 helper extractions land at cutover. **REMAINING: the real-play soak (normal sessions, zero effort — a "tag-shadow diff" toast means evidence captured) → then the cutover commit.** |
| UA2 | ⛨ **Phantom tag `[ENEMY_SURRENDERS]`** — present in both `_CT_TAGS` and `_CT_BARE` (api.js:417–418) so it strips from display, but has NO `applyMuts` handler and is absent from the STATE TAGS docs. Strippable, undocumented, inert — living proof of the UA1 triple-source drift. Decide: implement (morale/surrender is a real combat beat) or remove from the strip lists. On the drift surface (tag vocabulary + cleanTxt). | S | Fable | ✅ **Decided: IMPLEMENT (user call 2026-07-10)** — surrender is a real combat outcome, especially for communicative NPCs. Semantics designed in [MULTI_ENEMY_COMBAT.md](MULTI_ENEMY_COMBAT.md) §3 (named form marks a foe `down:"surrendered"`, bare form surrenders all living foes, all-down closes as "surrender", GM reminded to register a surrendered speaker via `[NPC:]`). Ships WITH UA26 post-cutover; until then the tag stays documented in `TAG_NO_HANDLER`. |
| UA3 | ⛨ **Transcript decompression failure is silent, permanent data loss.** `parseWorldState` (state.js:46–61): if the `{__lz:…}` inflate throws or `LZ` is missing at load (script-order/SW-cache skew), the catch replaces the transcript with `[]` and only console.errors — the verbatim record RAG + the story compiler depend on is gone, invisibly. Make it LOUD (toast + refuse to overwrite the stored blob with a transcript-less save until acknowledged); consider keeping the compressed blob under a recovery key. **⚠ guard:** the fix must not be able to brick saving on a false positive — the refuse-to-overwrite path needs an explicit user escape hatch. | S | Fable | ✅ **Done (v1.234)** — preserve-and-restore (no save-bricking, no escape hatch needed): failure stashes the compressed blob under a per-campaign rescue key (`TRANSCRIPT_RESCUE_K`), loud toast + console; `restoreTranscriptRescue()` re-inflates on a later healthy load and PREPENDS with an overlap guard, running BEFORE loadState's migrate-save (which would otherwise persist the emptied record). Pre-review found a 2nd failure mode the review missed: LZ-absent left the raw `{__lz}` object poisoning the transcript (push throws) — same rescue path now covers it. 5 engine tests (257 total). |
| UA4 | ⛨ **`rerollLast` desyncs the summarize marker.** It pops two `sessionLog` entries (game.js:433–463) without touching `worldState.sessKept`; the `k<=sessionLog.length` fail-safe (memory.js:631) catches shrinkage past the marker but a stale marker can still mis-slice the extraction window / retained tail. Adjust `sessKept` on pop (or clamp explicitly). **⚠ guard:** this REPAIRS a drift protection (#28 tail retention) — a wrong fix mis-slices extraction (double-files or loses exchanges). Test-first: an engine test spanning reroll-across-summarize-boundary BEFORE the fix. | S | Fable | ✅ **Closed as PHANTOM (v1.235)** — the session-C pre-review read the code and found `rerollLast` restores `sessionLog` length on BOTH paths (success pushes the swapped pair, failure restores the originals), so the marker never invalidates. No drift-surface surgery performed; the invariant is pinned by an engine test + comment (258 total). Residue accepted: a re-rolled response never reaches a future extraction (the discarded original was extracted; same scene). |
| UA5 | ⛨ **Stable-prompt purity has no runtime guard + cache visibility is thin.** The stable/volatile invariant (api.js:212–219) is enforced only by one engine test; one leaked per-turn value silently zeroes every cache hit — a pure cost regression with no functional symptom. Add: ① a dev-mode runtime assert (hash the stable half turn-over-turn, warn on change mid-campaign), ② a cache-read ratio line in the Usage modal so a dead cache is visible, ③ fix the stale globals.js comments still saying cache fields are "0 until #11 lands" (#11 shipped v1.151 — comment rot that misled this very review). **Sequencing note:** build BEFORE UA28 — it's the tripwire that catches a reinforce block landing in the wrong prompt half. | S | Fable | ✅ **Done (v1.236)** — `_checkStablePurity` (api.js): djb2 hash of the stable half per gameplay call (hashed AFTER the reinforce append = exactly what's sent), console.warn on every mid-campaign change + once-per-session toast; campaign switches reset cleanly. Usage modal gained a color-coded "Prompt-cache health: N% of turn input served from cache" line (0% = dead cache, red). Comment rot fixed (globals.js + ui.js both claimed #11 hadn't shipped). Live-verified against the real buildSysPrompt: turn/hp/location churn → 0 warns; a rules edit → exactly 1. Engine test (259 total). UA28 is now safe to build. |
| UA6 | ⛨ **Mid-turn persistence window.** `applyMuts` ends with `saveAll()` (api.js:739) — state persists BEFORE `sendAction` pushes the GM turn to `sessionLog` (game.js:416) and `logTranscript` (414). A throw in that window persists mutated worldState whose history lacks the assistant message → next prompt is desynced from state. Move the save after history append, or make the tail render steps throw-safe. **⚠ guard:** touches sessionLog/transcript ordering — the summarize + RAG substrate; verify against the `_committed` retry-guard semantics and the transcript's no-duplicate-player-line invariant. | S | Fable | ✅ **Done (v1.235)** — success path reordered: `logTranscript` → `sessionLog.push` → `saveAll` now run BEFORE the display steps (addMsg/TTS/actions). Fault-injection verified in preview (addMsg throwing on the narrator render): OLD code left hp mutated with sessionLog empty + transcript missing the GM turn; NEW code leaves state+history consistent in memory AND persisted, "display step failed" message intact, and reload replays the missed narration. `_committed`/retry semantics unchanged. |
| UA7 | **XP-mirror dedup relies on object identity.** `_xpSkip` holds `charSheet` references (api.js:583–594, 730); any future path that clones/reassigns a companion sheet mid-`applyMuts` silently double-awards. Key by companion name instead, or pin the invariant with a comment + test. | S | Any | Open — session D |
| UA8 | **`[HP:]` has no NaN guard** (api.js:510) — migration heals maxHp (state.js:89) but a save escaping migration can drive hp to NaN permanently. One `isFinite` clamp. | S | Any | Open — session D |
| UA9 | ⛨ **`currentNodeKey()` helper missing** — `world.sublocation ? loc+"\|"+sub : loc` is hand-computed ~8× (api.js:6–7, 535; memory.js:92, 98, 109, 116, 317). Each copy is a divergence site for sublocation-keyed map bugs — and the map keys ARE the geography canon (`[LOCATION_DESC:]` write-once, GEOGRAPHY block). Extract once (helpers.js). | S | Fable | Open — rides UA1 |
| UA10 | ⛨ **Schema knowledge triplicated across `blankMemory` / `healMemory` / `migrateWorldState`** (state.js:21, 135–167, 85–116) — construct / repair-memory / repair-worldState each hand-encode the same shapes; adding a field means touching all three plus inline literals (the drift `blankMemory` was created to stop). Consolidate onto one schema descriptor that all three derive from. **⚠ guard:** these repair the memory tiers on every load — a consolidation bug corrupts memory shapes silently on next open; migration idempotency tests against real exported saves required. | M | Fable | Open |
| UA11 | **Silent-failure swallows inventory** — `callGM` usage-parse `catch(e){}` (api.js:788), `generateActions` swallows-all into cleanup (game.js:85), `loadRules`/`saveRules` (api.js:41–42), `parseCompanionSheet` null-on-any-throw (game.js:270). Contradicts the stated policy while structurally similar failures shout. Make loud or annotate each as deliberately quiet with the reason. | S | Sonnet | ✅ **Done (v1.237)** — all four now warn with consequences named: usage-parse ("this call is uncounted — #30 undercount"), generateActions ("buttons removed, turn succeeded" — UI stays deliberately quiet), saveRules (+ toast — a lost custom rule is player-visible), loadRules, parseCompanionSheet ("stub fallback will be used"). |
| UA12 | ⛨ **`resolveNpcName` heuristic is load-bearing and under-documented** (memory.js:16–41): single-candidate token-subset auto-resolve; adding a second NPC sharing a distinctive token mid-campaign changes resolution behavior for previously-clean names. Also O(n·tokens) inside hot loops. Document the invariants + edge tests (two-same-token NPCs, reverse-direction fork); perf memoization if profiling warrants. The alias system is the identity spine of NPC memory — the one-time dupe MERGE of existing forked saves is separate (UA29). | M | Fable | Open |
| UA13 | **Usage telemetry can't support subscription pricing yet** — `usageCost` prices only Anthropic prefix-matches (api.js:744–749, $0 otherwise), AND OpenAI-family `prompt_tokens` INCLUDES cached tokens while Anthropic excludes them (globals.js:23–24), so cross-provider totals aren't the same unit. **Enriches #30**: fix both when doing #30 — per-provider pricing + unit normalization + the "unpriced calls: N" visibility line. | S | Sonnet | Open — do WITH #30, session D |
| UA14 | ⛨ **`memory.archive` is write-only** (state.js:21; memory.js:135–142) — evicted lore/decisions/chapters compact into it, nothing reads it. It exists FOR Core Memory #40 / RAG; the consumer decision belongs to #40's design. Until then it's storage cost with no payoff. | — | Fable | ✅ **Resolved inside #40 (v1.243)** — decision: archive is **NOT** a Core Memory source (moment quality must be captured at write time by the engine triggers, not reconstructed from compacted evictions). Archive's designated consumers: the story compiler (#5) for enrichment + candidate document set for a future RAG phase; it also now RECEIVES #40's over-cap evictions (`archive.coreMemories`) so "never forget" degrades to cold storage, not loss. Still read by nothing at runtime — by design, until #5. |
| UA15 | **Transcript growth is mitigated, not fixed** — LZ halves the blob (v1.227) but the in-memory array, export, and sync blob keep the full record, and RAG linearly scans it each turn (memory.js:402–420). Watch; the documented trigger (quota bites again in the wild) → IndexedDB offload first. | — | — | Watch — trigger documented in Architecture ▸ JSON-vs-SQLite |
| UA16 | ⛨ **Skeleton progression can't self-correct** — `[ARC_COMPLETE]`/`[ACT_COMPLETE]` only advance (api.js:691–725); no reopen/reorder path, and the all-arcs-done / over-budget nudges are prompt-only pleas. The engine can DETECT the stall deterministically but only the GM can move state — the standing determinism gap. Design question: should engine-detected "all arcs complete + N turns idle" auto-advance with a narrated hand-off? **⚠ guard:** an engine auto-advance is a stronger intervention than the #23 nudges and can OVER-RAIL — advancing act state while the GM is mid-crisis is narrative-vs-state desync. Design discussion first, and only after #23's live compliance is observed. | M | Fable | Watch — design discussion after #23 compliance data |

## UI & support layer findings (ui.js · char-creation.js · storage-adapter.js · satellites)

| # | Task | Effort | Tier | Status |
|---|---|:---:|:---:|---|
| UA17 | **ui.js decomposition** — 3,227 lines, ~150 functions, ~18 distinct subsystems in one flat namespace (portrait-drag · folder-export · toasts/addMsg · syncUI+8 panels · server modal · import/export · char sheet · NPC sheet · browsers · campaign picker · companions · Car Mode · file menus · wireButtons · provider/usage/RAG/prose/quest modals · bootstrap). Biggest single targets: `showCharSheet` (~386 lines — a character sheet AND a full fal.ai portrait-gen sub-app in one function, ui.js:969–1409) and `wireButtons` (~180 lines of flat addEventListener where one early throw kills all later bindings, ui.js:2699–2881). **Split of labor: Fable draws the seam map (which functions move to which new file, what shared state each needs — the reviewers' table is the draft); Sonnet executes file-by-file** (load-order additions to index.html + sw.js precache per split). No behavior change; pure decomposition — drift-neutral. | L | Sonnet | Open — ui.js track; seam map first |
| UA18 | **escHtml sink list — the real #22 exposure, enumerated.** The story path is SAFE (all narration flows `escProse`); the gap is ~9 sinks: spell panel `nm`/`ds` raw (ui.js:411–412 — the adjacent ability panel DOES escape, drift); combat statblock immune/resist/vuln raw (ui.js:443–445); `alt='…'` attribute injection with PC name (ui.js:1073) and **model-invented NPC name** (ui.js:1569); char-creation review card name/appear (char-creation.js:187, 191, 205, 208); imported save name into `addMsg` (ui.js:803). **Enriches #22** — with this list #22 drops from M to S and needs no further investigation. | S | Sonnet | Open — do as #22, this row is its spec; ui.js track |
| UA19 | ⛨ **Sync last-writer-wins cannot survive multiplayer/server-authority** — `load()` adopts on `serverTurn > localTurn`, `_syncNow` POSTs whole state; no merge, no vector clock; portrait transport needs THREE cooperating mechanisms (turn-gate + portraitVer + fill-only), every branch annotated with a past incident (storage-adapter.js:284–336, 382–405, 407–525). Near-term: the **CAS turn guard** (Known issue #5, decision locked) stops silent clobbering. The real rewrite (changesets / server-authority) is gated on UA33's state-authority decision — do NOT redesign sync before that lands. On the drift surface via blast radius: a wrong reconcile replaces the memory/transcript wholesale. | — | Fable | CAS half ✅ **shipped (v1.238 + server deploy)** — see Known issue #5 in TODO.md for the full verification trail; also surfaced: **the server codebase is NOT under version control** (no .git — filed for UA32/session B). The LWW read-side rewrite remains gated on UA33's state-authority decision. |
| UA20 | **Campaign-list deletion never propagates** — `syncCampaignList` merges server-wins-on-ID but keeps local-only entries, so a campaign deleted on the server resurrects from any device that still has it (storage-adapter.js:340–371). Fine solo, wrong under authority; cheap tombstone or server-list-is-truth flag. | S | Sonnet | Open — session D |
| UA21 | **UI duplication cluster** — three segmented browsers each re-declare `segBtn`/render/wireSegs (blueprint ui.js:823, character 1586, companion 2317); two fal.ai portrait-gen modals (showCharSheet internals 1173–1310 vs char-creation.js:96–174); party render ×3 (updatePartyPanel 343, updateHUD cards 299–318, Car Mode 2487); THREE escapers now exist (helpers `escHtml`, `buildNarrativeHtml` local, tts.js `_escVal`); every menu-checkbox consumer hand-enumerates the three `fm-`/`cs-fm-`/`api-fm-` prefixes (stt.js:122, ui.js:497–499, 534). Consolidate per-cluster; natural companion to UA17. | M | Sonnet | Open — bundle with UA17 execution |
| UA22 | **PC avatar lacks the E60 offset fallback the NPC sheet has** — `refreshNpcAvatar`/`npcGetOff` (ui.js:1563–1585) gained the fallback; the parallel PC path (1070–1100) didn't. Small divergence instance of the showCharSheet/showNpcSheet copy-pasta. | S | Any | Open — session D, or dies naturally in UA17's shared sheet renderer |

## Audit-corpus open items (not already tracked in TODO.md)

| # | Task | Effort | Tier | Status |
|---|---|:---:|:---:|---|
| UA25 | ⛨ **Companion spell tracking** — `[SPELL_USED:]` matches only the PC; companions cast for free (v1.224 S1), and Haiku emits `[COMPANION_SPELL_USED:]` unprompted for a tag that doesn't exist (H5 — the model is asking for it). Add the tag + companion spell-bible canon injection (their spells live on `charSheet.spells` — the v1.224 B1(b) gap that also blocks the bible money test on imported casters). **⚠ guard:** adds tag instructions to the stable half AND a per-companion canon block to the volatile half — measure volatile size before/after (prompt-saturation is the historical rule-collapse trigger); stable-half edit = one-time cache invalidation, verify byte-identity resumes. | M | Fable | Open — after UA1 (additive there) |
| UA26 | ⛨ **Multi-enemy combat** — the engine holds exactly ONE combat object; 18/150 Haiku turns emitted a second `[COMBAT_START:]` (lost) and 12 named `[ENEMY_HP:Name\|-X]` forms were dropped (H2). Design the multi-foe combat shape (array of foes, named ENEMY_HP addressing, panel rendering) — a real design decision (initiative? shared morale? panel space on mobile?), then an additive build. **⚠ guard:** changes the combat block in the volatile half + COMBAT tag docs in the stable half + needs `migrateWorldState` for old single-foe saves; same prompt-change discipline as UA25. | M | Fable | ⚙ **Design doc written 2026-07-10** — [MULTI_ENEMY_COMBAT.md](MULTI_ENEMY_COMBAT.md): additive `foes[]` shape, COMBAT_START-adds-a-foe (the H2 fix), named ENEMY_HP addressing, auto-clear generalized to all-foes-down, migration + read-site enumeration, Haiku-corpus expected-diff replay as validation. **✅ All 4 decisions RATIFIED by the user 2026-07-10** (§7 of the doc): initiative stays GM-narrated; bare ENEMY_HP routes to the ENGAGED foe (`combat.engaged` = last foe the player damaged) with first-living+warn fallback; **ENEMY_SURRENDERS is IMPLEMENTED, not deleted** (user overrode the delete rec — surrender is a real beat for communicative NPCs; resolves UA2 as implement); foe cap 8. Build-ready at cutover; stable-half doc edits bundle with UA25 (one invalidation). |
| UA27 | ⛨ **Combat tag with null combat should warn loudly** — the v1.224 C1 class: premature `[COMBAT_END:]` then subsequent combat tags silently no-op. Engine half of the C1 remedy. | S | Fable | Open — subsumed by UA1's per-tag `needsCombat` guard |
| UA28 | ⛨ **Haiku free-tier prompt nudges** — H1 (HP recovery under-emitted: sheet sat at 0 HP for 31% of turns) and H3 (location under-emitted) want a Haiku/weak-model `reinforce` block (the per-provider mechanism already exists and Anthropic's adapter currently sends none — add a model-conditional variant). Then re-run a Haiku window to validate. Free tier = the subscription funnel; these findings ARE free-tier product quality. **⚠ guard (the sneaky one):** WHERE the reinforce lands is load-bearing — if it perturbs the stable half, byte-identity breaks and every cache hit dies silently; and it must NOT displace STYLE from the very end (end-position is load-bearing for prose-voice fidelity, audit #2). Build UA5's tripwire FIRST so a placement mistake is caught. | S | Fable | ✅ **Shipped (v1.242, 2026-07-10; pre-review presented + approved same day)** — `resolveReinforce(prov,model)` (api.js): reinforce may now be a function of the model id; `anthropic.reinforce` returns `ANTHROPIC_HAIKU_REINFORCE` (globals.js — H1 HP-recovery + H3 location discipline ONLY, deliberately not the full TAG_REINFORCE) for `/haiku/i` ids and `""` otherwise. Sonnet/Opus prompt BYTE-IDENTICAL (engine-tested against the live buildSysPrompt — zero cache invalidation for existing campaigns); Haiku block appends to the stable TAIL (cache-constant per model), volatile + STYLE-at-end untouched; all 5 providers resolve. 5 engine tests (275 total). **REMAINING: the fresh Haiku validation window** — rides the post-batch playtest per the spine. **First window datapoint (t361 Ammut, 2026-07-10, v1.247, stamped `haiku-4-5`): H-NEW — spell-canon disobedience.** Haiku ran a multi-exchange Message conversation at THREE MILES (the exact t308 drift case Sonnet refuses under bait, v1.238 money test) — ignoring BOTH the every-turn bible canon (#10) and v1.245's suggestion range fence (the button itself offered "contact someone outside this location"; suggestions run on the active model too). Sonnet-validated injections are necessary but not sufficient on Haiku. **Remedy ✅ shipped (v1.248, same day, user go):** item ③ in `ANTHROPIC_HAIKU_REINFORCE` — spell-canon discipline ("hard physics… an over-canon cast simply FAILS; narrate the failure and offer what canon allows"). Haiku-only stable-half append (Sonnet byte-identity unaffected by construction — resolver returns "" for non-Haiku); one-time cache reset on Haiku campaigns; pin test added (305 total). **The remainder of the live Haiku window now measures the corrected block** — watch for a repeat Message/range violation (stamped turns make the tally trivial). **⚖ VERDICT (user call, 2026-07-10, end of the t356–383 window): Haiku is NOT VIABLE as GM for this product.** Four independent legs: canon ignored under pressure (t361 three-mile Message), prompt fences ignored (t371 ghost ship), contradictory canon amplified rather than reconciled (t378 woman-in-bronze), targeted cleanup instructions ignored (the Daeris condition test) — plus 3 of the session's 4 retcon incidents. \"The difference in drift and hallucination compared to Sonnet is obvious.\" Item ③ ended unvalidated (no second long-range attempt) — moot for the product decision. **Product direction: free tier = CAPPED SONNET TURNS, not a weaker model** (~80 Sonnet turns ≈ $2.88 ≈ 150 Haiku turns; quality constant, quantity limited — the free taste plays like the paid product). The reinforce machinery (incl. items ①–③) is RETAINED for gpt-4o/Gemini-class providers and any future weak-model experiments; no further Haiku-specific investment. |
| UA29 | ⛨ **One-time NPC dupe merge on mature saves** (t308 F1 — Morwen ×3, Hemlock ×3…): prevention shipped (v1.62 + resolveNpcName guard) but existing forked keys in old saves still fragment memory. A data-surgery script (node, over an exported `.tnd`): absorb short→full, merge knowledge/events/aliases, rewrite relationship references, re-import. RAG currently masks the damage; structured memory stays fragmented. **⚠ guard (data surgery on the memory graph):** a wrong merge fuses two genuinely distinct NPCs (the two-Aldaras hazard) — run on an exported COPY, produce a human-reviewed merge report BEFORE re-import, keep the original; verify post-merge that RAG's write-time `e.n` index still resolves through the merge-orphan bridge (don't assume). | M | Fable | Open — script not started |
| UA30 | ⛨ **0/0-objective quests read ambiguously** by the #20 all-objectives-complete teeth (t308 F4) — a quest with no objectives is "all complete" vacuously. Guard: require ≥1 objective before the close instruction fires, plus a nudge to file objectives on objective-less actives. Touches `buildQuestBlock` — the quest lifecycle teeth. | S | Fable | Open |
| UA31 | ⛨ **Arc↔quest lifecycle coupling** — `[ARC_COMPLETE:]` fired while the twin quest never closed (AUDIT_PLAYTHRU, filed unresolved). When an arc completes, nudge a questLog entry whose title matches the arc. **⚠ guard: NUDGE, not auto-close** — engine-closing a quest the GM hasn't narrated closed creates narrative-vs-log desync (the quest block would drop it mid-story). | S | Fable | Open |
| UA32 | **Server `index.js` has never been audited** — the only major surface with zero audit coverage (AUDIT_PLAYBOOK names it), and it's about to become the subscription gateway. Survey endpoints/schema/auth/failure paths FIRST — as the opening act of UA33. | M | Fable | Open — session B, step 1 |
| UA33 | **`SERVER_ARCHITECTURE.md`** — the queued design doc (Architecture ▸ "Server & services shape"): LLM proxy (streaming, per-tier rate limits, metering, runaway-cost guards), billing (Stripe? tiers? lapse behavior), auth evolution (GitHub OAuth → subscription entitlement), **state authority** (dumb blob store vs authoritative server — decides multiplayer, CAS generalization, and where UA1's table ultimately runs), deploy/scale, Electron auth. Pure design, zero code risk, everything on the Clear-for-Release path hangs off it. | L | Fable | Open — **session B** |
| UA34 | **Stale-status reconcile** — AUDIT_FABLE_07_01 #24 (`_applyBlueprint` dead selector) still says Pending while BLUEPRINT_EDITOR.md §5.2 records it closed at v1.156. Verify in code, fix the stale row. Tiny, but stale audit rows poison future reviews (this one had to hedge around it). | S | Any | Open — session D |
| UA38 | ⛨(① only) **Exits-as-canon in `[LOCATION_DESC:]` + scene-grounded action suggestions.** Live incident (t333 Ammut, 2026-07-10): a suggestion button invented a lockable exit no narration ever mentioned. Root cause in `generateActions` (game.js:66–75): the suggestion model sees only the FIRST 2,400 chars of the latest GM message + the kit digest — no GEOGRAPHY canon, and the prompt's only hard fence is capabilities (the Magic-Missile fix); nothing forbids inventing *scenery*. Untaken, a hallucinated suggestion is a drift **injection vector**: tapped, it arrives as player intent and pressures the GM to invent the door, which then files as canon. Package: ① add "always include every visible exit and where each leads" to the `[LOCATION_DESC:]` doc line (⛨ stable half — one-time cache invalidation; post-cutover it's a one-line `TAG_DOC_LINES` edit); ② feed the current node's stored desc into the suggestion call + a "suggest only actions involving people, objects, and exits explicitly present; never invent scenery" line (drift-neutral, game.js only); ③ `slice(0,2400)` → `slice(-2400)` so the model sees the scene's END, not its head (prose-voice responses regularly exceed 2,400 chars and the tactical present lives at the tail). **Watch:** write-once means a first-visit omission is permanent — if that bites in play, the upgrade is append-only `[EXIT:]` entries (post-UA1 additive). Structured full-geography breakdown evaluated and REJECTED 2026-07-10 (over-commits the narrator at first sight; prose + re-injection already carries flavor — see conversation record). | S | Fable ① / Sonnet ②③ | ②③ ✅ **Shipped (v1.245, 2026-07-10 — user promoted out of FOR EXPLORATION)**: `suggestionGeoLine()` feeds the current node's canonical desc (sublocation-first) into the suggestion call with a hard "NEVER invent doors, exits, items, or people" instruction; `suggestionSceneTail()` keeps the message END (`slice(-2400)`). 5 engine tests (293 total); live validation = watch the buttons next session. ① (exits in the LOCATION_DESC doc line, ⛨ stable half) still rides the cutover-batch invalidation. |
| UA39 | **Suggestion buttons recommend canon-ILLEGAL casts, and the GM lacks distance grounding to refuse them** — 2nd live suggestion-quality incident (t355 Ammut, 2026-07-10): a button offered "Use Message to contact the healer in town immediately" (target across Sandpoint, far past Message's 120ft), the player tapped it, and the GM complied. Two gaps, one chain: ① `generateActions`' sheet digest lists spell NAMES only — the Magic-Missile fence blocks spells you don't *have*, nothing blocks casts canon *forbids* (the suggestion model doesn't know Message HAS a range). ② The GM enforces ranges stated as distances (money test: refused at miles, "doesn't bend around corners") but has NO numeric distance model for scene geography — "across town" vs 120ft is a judgment call it got wrong under player-intent pressure, the same intent-laundering vector as UA38. Note what still worked: the single whisper-reply then "the Message cuts off" is correct cantrip mechanics — the failure is distance judgment, not canon amnesia. Remedies: ① (drift-neutral, rides UA38 ②③) feed compact per-spell canon lines (range/targets via `capabilityLookup`) into the suggestion call + "only suggest casts within the listed limits"; ② (⛨ stable half, cutover batch) give the GM range-judgment grounding — candidates: surface `LOCATION_SIZE` travelMins in the GEOGRAPHY block ("crossing this location ≈ N min on foot"), or a bible-block header line ("a target in another building/street/district is beyond short spell ranges unless the fiction says otherwise"). | S | Fable ② / Sonnet ① | ① ✅ **Shipped (v1.245, 2026-07-10)**: `suggestionSpellList()` annotates every available spell with its bible canon ("Message (range 120 feet, targets 1 creature you have seen…)") via `capabilityLookup`, + a "never suggest a cast exceeding listed range/targets (another building/street is beyond a short-range spell)" fence. Engine-tested on the exact t355 input (Message). ② (GM-side distance grounding, ⛨ stable half) still rides the cutover-batch edit. **t371 datapoints (Haiku window, v1.247 — fences ACTIVE):** ①-fence ignored — button offered "Message someone to check on the ship's status" with NO ship in scene (GM itself confirmed "there's no ship"), then "Use Message spell to contact Shalelu" AGAIN (she's miles away tracking goblins — the un-retconned t361 precedent visibly feeding the suggestion model, exactly as predicted). Conclusion: prompt fences in a 200-token utility call are Sonnet-grade; Haiku ignores them. **Structural fix ✅ shipped (v1.249, user go — "good call")**: `upgradeModelFor()` (game.js) — the skeleton's inline escalation extracted to ONE helper, now used by BOTH the skeleton and `generateActions`; gated by the existing "Allow model upgrade for complex tasks" toggle, per-provider `upgradeModel`, ≈$0.002/turn. Suggestion quality no longer degrades with the free tier; the fences remain for the Sonnet path they're proven on. Engine test covers escalation + toggle-off (306 total). |
| UA40 | ⛨ **Reveal-commitment gap — an identity reveal never updates structured memory** (t378 Ammut, 2026-07-10, the woman-in-bronze incident). Established canon (Daeris IS the woman in bronze, confirmed ~t328 — the chapter summary records "yes, that's her") coexisted at t359 with: ① a stale PRE-reveal hedge in `memory.npcs["Daeris"].knowledge` ("has not confirmed or denied"), and ② a fully FORKED `"Woman in Bronze"` NPC entry, never aliased/merged. The prompt served CONTRADICTORY canon — story recap said confirmed, the authoritative-looking NPC-details block said not — and Haiku narrated them as separate people. The model amplified the stack's contradiction; the commitment failure is ours. Three design gaps: (a) merge/alias emission at reveal moments depends on GM judgment (the historically spotty class — quest-close, FUTURE_EVENT_RESOLVED); (b) **knowledge has no supersession** — extraction appends, a contradicting reveal never retires the hedged fact it obsoletes; (c) revelations are the defining-moment class engine triggers CANNOT detect — the strongest argument yet for prioritizing the deferred `[CORE_MEMORY:]` GM tag at cutover (#40 Phase 2). Remedies to design: reveal-nudge in the extraction prompt (echo superseded facts for retirement, same pattern as #29's resolvedEvents), a `[NPC_SUPERSEDE:name\|old fact\|new fact]`-shaped op or NPC_FORGET+NPC_NOTE convention, UA29's merge script for existing forks. In-game repair line issued (merge + forget + retcon). | M | Fable | Filed 2026-07-10 — design at/after cutover; case study preserved in saves t359/t378 |

## Money tests & validation runs outstanding (play sessions, not model work)

| # | Task | Effort | Tier | Status |
|---|---|:---:|:---:|---|
| UA35 | **Spell-bible obedience money test** — never actually exercised in three runs: imported/blueprint casters bypass the catalog injection path, so nothing injected was ever cast. Run with a **freshly wizard-built catalog-spell caster** (cast Message / Hunter's Mark under pressure, watch the fixed bounds hold). The single load-bearing open validation of #10. | S | Any | ✅ **PASSED (2026-07-09)** — 40-turn Sonnet harness run, fresh catalog Arcane Trickster (the only carrier of BOTH historical drift spells). Both drift incidents dead: Message refused at miles TWICE incl. an explicitly baited retry ("the limit doesn't care about your circumstances"), Mage Hand dissolved at 30ft, + 8 unprompted canon holds in random turns. Full report: [AUDIT_playtest_v1238.md](AUDIT_playtest_v1238.md); corpus: dev/corpus_playtest_v1238.json. New findings filed there: F1 slot-expenditure softness (root cause api.js:141 — sheet OMITS used spells instead of naming them; ⛨ fix proposed), F3 quest-reopen double-pay. Residual: Hunter's Mark exclusivity (Ranger) untested — short follow-up run if wanted. |
| UA36 | **RAG A/B matrix** — {Sonnet, Haiku} × {flag on, off} on a mature save (RAG_MEMORY §9). Never run; §7 warns never to judge RAG on a fresh harness. Also wants the harness RAG-block capture enabler (`window.__lastRagBlock`, ~30 min — build first, it's the prerequisite for the parked stale-chunk tripwires too). | M | Any | Open — harness session; enabler is Sonnet |
| UA37 | **Validation batch** — Gemini live-key confirm (E89 tokScale), SW offline + simulated-502 on a real device (Designer #5), v1.214 F2/F3 prompt-half live compliance, #23 guided-path live compliance (post-v1.231 playthrough), Car Mode + Bluetooth in a real car (#2/#19). | — | Any | Open — accumulate into the next real play sessions |

## Validated & healthy — do NOT re-litigate or refactor

- **Working, proven at scale:** quest lifecycle close+archive (t308/v1.214/v1.224), NPC registration 97% at t308, futureEvents hygiene, prompt caching (−29% turn cost, 11/11 hits), RAG Phase 1 (default-on validated), companion sheets/XP mirror/level-up, tag-contract discipline even on Haiku (0 turn errors/150), summarize tail retention, no raw-tag leakage.
- **Clean code, leave alone:** the `PROVIDERS` adapter table (the model UA1 should imitate), `store` + in-memory fallback, `switchToCampaign` rollback, companion-sheet generation pipeline, inventory stacking helpers, `migrateWorldState` pattern, `compress.js`, `sw.js`, `tts.js`/`stt.js` IIFEs, `helpers.js` purity, the `escProse`/`addMsg` story contract.
- **Cost picture:** Haiku ≈ $0.019/turn, Sonnet ≈ $0.036/turn; a 150-turn campaign ≈ $2.82 on Haiku — the free-tier economics work if UA28's quality gaps close.
