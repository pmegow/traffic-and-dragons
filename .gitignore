# Auto-exported narrative files
*.txt
!dev/golden_stable_PRE.txt

# OS junk
.DS_Store
Thumbs.db

# Local backups
BAK/

# Audit archive: the root AUDIT_*.md evidence files moved here 2026-07-13 and STAY TRACKED
# (they are the cited evidence base for TODO.md / Fable_UberAudit.md rows). Only the Fable
# working audits remain local-only, as before.
audits/AUDIT_FABLE_*.md

# Personal save files
*.tnd

# Campaign export folder (File System Access target — saves, renders, narratives written by the
# game itself). Live play output, never source. Was merely untracked until 2026-07-27, which meant
# it showed in every `git status` and one stray `git add -A` would have committed it.
Campaigns/

# Exported narrative mementos (#5 story compiler output) — personal play output, same class as
# Campaigns/ (user call 2026-08-15)
momentos/

# Personal Claude Code settings (hooks + launch.json ARE committed)
.claude/settings.local.json

# Bug-triage endpoint secret (/bugs sync) — the GAS doGet shared secret never enters the repo
.claude/bugs.local.json
dev/*.endstate.json
!dev/corpus_playtest_v1238.json.endstate.json
!dev/corpus_playtest_v1258.json.endstate.json
!dev/corpus_playtest_v1271.json.endstate.json
!dev/corpus_playtest_v1276.json.endstate.json

# exploratory lens reviews (owner-only reading, never shipped)
gygax_lens_results.html
