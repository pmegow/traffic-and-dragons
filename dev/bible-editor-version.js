// Visible browser-UI revision. Helper compatibility is versioned independently so UI-only
// releases do not require terminating the detached local writer.
var BIBLE_EDITOR_VERSION = "1.1.6";

if (typeof module !== "undefined" && module.exports) module.exports = BIBLE_EDITOR_VERSION;
