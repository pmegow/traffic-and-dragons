// One revision for the browser UI, local helper, and launcher compatibility check.
// Bump this whenever their shared workflow changes; the visible label makes stale tabs obvious.
var BIBLE_EDITOR_VERSION = "1.1.5";

if (typeof module !== "undefined" && module.exports) module.exports = BIBLE_EDITOR_VERSION;
