// Isolated transport and scheduler fixtures: no paid requests or real audio.
const assert=require('assert/strict'),engine=require('./load-engine.js');engine.loadEngine();
const realTimer=global.setTimeout,realFetch=global.fetch;
const sleep=ms=>new Promise(r=>realTimer(r,ms));
const calls=[],sources=[],toasts=[];
global.document={getElementById:()=>null,addEventListener:()=>{},removeEventListener:()=>{}};
global.window={AudioContext:function(){this.state='running';this.currentTime=0;this.sampleRate=24000;this.destination={};this.createGain=()=>({gain:{},connect(){}});this.createBuffer=(c,n,r)=>({duration:n/r,getChannelData:()=>new Float32Array(n)});this.createBufferSource=()=>{const src={context:this,connect(){},disconnect(){},start(){if(!this.loop)sources.push(this)},stop(){},buffer:null};return src;};}};
global.showToast=m=>toasts.push(m);providerKeys.openai='synthetic-test-key';
let passed=0;async function test(name,fn){try{TTS.stop();TTS._openai.reset();calls.length=0;sources.length=0;await fn();passed++;console.log('PASS '+name)}catch(e){console.error('FAIL '+name+' — '+e.message);process.exitCode=1}finally{TTS.stop();global.setTimeout=realTimer;global.fetch=realFetch;}}
async function settled(p){let timer;try{return await Promise.race([p,new Promise((r,j)=>{timer=realTimer(()=>j(Error('operation stayed pending')),250)})])}finally{clearTimeout(timer)}}
const pcm=()=>({ok:true,status:200,arrayBuffer:async()=>new Uint8Array([0,0,255,127]).buffer});

const S=TTS.settings;
function draft(id){const d=S.draft();d.primary=id;d.keys[id]='fixture';d.models[id].voices=[{id:'a',label:'Actor A',g:'F',note:'Warm'},{id:'b',label:'Actor B',g:'M',note:'Deep'},{id:'c',label:'Actor C',g:'F',note:'Clear'}];d.models[id].narrator='a';return d;}
(async()=>{
 for(const id of ['inworld','speechify']){
  await test('#401 '+id+' uses documented PCM request and direction controls',async()=>{const d=draft(id),c=d.models[id];c.direction='Whisper gently';c.rate=1.1;c.emotion='calm';c.language=id==='inworld'?'ko-KR':'en-US';let request;global.fetch=async(url,opts)=>{request={url,opts,body:JSON.parse(opts.body)};return id==='inworld'?{ok:true,json:async()=>({audioContent:Buffer.from([0,0,255,127]).toString('base64')})}:pcm()};let r=await S.fetch(id,{text:'문을 열어. <hello> & friends',voice:'a'},c,'fixture');assert.equal(r.rate,24000);assert.equal(r.bytes.length,4);assert.equal(request.opts.headers.Authorization,(id==='inworld'?'Basic':'Bearer')+' fixture');if(id==='inworld'){assert.equal(request.body.modelId,'inworld-tts-2');assert.equal(request.body.instruction,'Whisper gently');assert.equal(request.body.language,'ko-KR');assert.equal(request.body.audioConfig.audioEncoding,'PCM');assert.equal(request.body.audioConfig.speakingRate,1.1)}else{assert.equal(request.body.model,'simba-3.2');assert.equal(request.body.output_format,'pcm_24000');assert(request.body.input.includes('&lt;hello&gt; &amp; friends'));assert(request.body.input.includes('emotion="calm"'));assert(request.body.input.includes('rate="110%"'));assert(!request.body.instructions)}});
  for(const phase of ['headers','body'])await test('#401 '+id+' bounds stalled '+phase+' and cancellation',async()=>{let signal;global.setTimeout=(fn,ms)=>realTimer(fn,Math.min(ms,15));global.fetch=(u,o)=>{signal=o.signal;return phase==='headers'?new Promise(()=>{}):Promise.resolve({ok:true,json:()=>new Promise(()=>{}),arrayBuffer:()=>new Promise(()=>{})})};const r=await settled(S.fetch(id,{text:'wait',voice:'a'},draft(id).models[id],'fixture'));assert.match(r.fail,/timeout/);assert(signal.aborted)});
  await test('#401 '+id+' refuses malformed audio and paid errors without retries',async()=>{for(const status of [401,402,429,500]){let n=0;global.fetch=async()=>{n++;return{ok:false,status}};const r=await S.fetch(id,{text:'wait',voice:'a'},draft(id).models[id],'fixture');assert.match(r.fail,new RegExp(String(status)));assert.equal(n,1)}global.fetch=async()=>({ok:true,json:async()=>({audioContent:'AQID'}),arrayBuffer:async()=>new Uint8Array([1,2,3]).buffer});assert.match((await S.fetch(id,{text:'wait',voice:'a'},draft(id).models[id],'fixture')).fail,/Invalid/)});
  await test('#401 '+id+' audition uses draft without saving and close cancels request',async()=>{const d=draft(id),before=JSON.stringify(S.draft());let request;global.fetch=(u,o)=>{request=o;return new Promise(()=>{})};S.test(d,'A quiet road.','b',()=>{});await sleep(10);assert.equal(JSON.stringify(S.draft()),before);assert(request);TTS.stop();assert(request.signal.aborted);await sleep(5);assert.equal(sources.length,0)});
  await test('#401 '+id+' failed audition never plays another engine or changes selected provider',async()=>{let requested=[];const d=draft(id),before=S.draft().primary;global.fetch=async(u,o)=>{requested.push(u);return{ok:false,status:401}};const phases=[];S.test(d,'A quiet road.','b',p=>phases.push(p));await sleep(20);assert.equal(requested.length,1);assert.equal(S.draft().primary,before);assert.equal(phases.at(-1),'idle');assert.equal(TTS.isPlaying(),false);assert.equal(sources.length,0)});
 }
 await test('#401 catalog follows even empty pages and filters unsupported Speechify actors',async()=>{let urls=[];global.fetch=async url=>{urls.push(url);return{ok:true,json:async()=>urls.length===1?{voices:[],has_more:true,next_cursor:'a b'}:{voices:[{id:'old',models:[{name:'simba-english'}]},{id:'new',display_name:'A <voice>',gender:'female',models:[{name:'simba-3.2'}]}],has_more:false}}};const v=await S.loadCatalog('speechify','fixture');assert.equal(urls.length,2);assert(urls[1].includes('cursor=a%20b'));assert.equal(v.length,1);assert.equal(v[0].id,'new');assert.equal(v[0].g,'F')});
 await test('#401 catalog rejects repeated cursor and supports explicit cancellation',async()=>{global.fetch=async()=>({ok:true,json:async()=>({voices:[],has_more:true,next_cursor:'loop'})});await assert.rejects(S.loadCatalog('speechify','fixture'),/repeated/);let c;global.fetch=()=>new Promise(()=>{});const p=S.loadCatalog('inworld','fixture',x=>c=x);c.abort();await assert.rejects(settled(p),/cancelled/)});
 await test('#401 Save commits selection and cast but never changes GM provider or NPC pins',async()=>{const d=draft('inworld'),gm=activeProvider,pins=JSON.stringify(worldState);d.models.inworld.cast['legacy#12']='b';S.save(d);assert.equal(TTS.getEngine(),'inworld');assert.equal(S.actor('inworld','legacy#12',S.draft().models.inworld),'b');assert.equal(activeProvider,gm);assert.equal(JSON.stringify(worldState),pins);let reload=S.draft();assert.equal(reload.keys.inworld,'fixture');assert.equal(reload.models.inworld.narrator,'a');reload.models.inworld.narrator='c';assert.equal(S.draft().models.inworld.narrator,'a');store.del(S.keys.settings);store.del(S.keys.credentials)});
 await test('#401 local draft keeps an imported composite narrator and Google auto-cast stays stable',async()=>{const d=S.draft();for(const v of TTS.starsList()){assert.equal(S.actor('gemini',v.id,d.models.gemini),TTS._gemini.voiceFor(v.id));assert.equal(S.actor('openai',v.id,d.models.openai),TTS._openai.voiceFor(v.id))}});
 await test('#401 storage quota failure restores every preference and credential',async()=>{const d=draft('speechify'),before=JSON.stringify(S.draft()),keys=JSON.stringify(providerKeys),original=store.set;let n=0;store.set=function(k,v){if(++n===3)throw Error('Quota exceeded');return original(k,v)};try{assert.throws(()=>S.save(d),/Could not save/);assert.equal(JSON.stringify(S.draft()),before);assert.equal(JSON.stringify(providerKeys),keys)}finally{store.set=original}});
 await test('#401 unstarred imported Piper speaker remains selectable and validates for Save',async()=>{const d=S.draft();d.primary='local';d.models.local.narrator='en_US-libritts_r-medium#611';assert.equal(S.validate(d),'');assert(S.catalog('local',d.models.local).some(v=>v.id===d.models.local.narrator));});
 await test('#401 a small actor bank reuses a matching narrator before crossing gender',async()=>{const d=draft('inworld'),c=d.models.inworld;delete c.cast[TTS.starsList()[0].id];c.voices=c.voices.slice(0,2);const female=TTS.starsList().find(v=>v.g==='F');assert.equal(S.actor('inworld',female.id,c),'a');});
 await test('#401 malformed saved root does not crash the settings dialog',async()=>{const k=S.keys.settings,old=store.get(k);try{store.set(k,'null');assert(S.draft().models.inworld);store.set(k,'[]');assert(S.draft().models.inworld)}finally{if(old===null)store.del(k);else store.set(k,old)}});
 console.log((process.exitCode?'FAILED':'ALL GREEN')+' — '+passed+' settings/transport groups');
})().catch(e=>{console.error(e);process.exitCode=1});
