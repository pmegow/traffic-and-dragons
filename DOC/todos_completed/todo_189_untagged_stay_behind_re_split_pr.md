# TODO #189 — Untagged stay-behind re-split + prose item misattribution — "Morwen's hand near Cleaver's hilt out of old habit" (field, t1833).

*Full record extracted verbatim from TODO.md (Feature backlog, completed row) on 2026-09-01; the tracker row keeps the TLDR and the verdict. Table pipe escapes (`\|`) are unescaped here; `<br>` breaks became paragraphs.*

**Effort:** M · **Tier:** Fable

## Task

**Untagged stay-behind re-split + prose item misattribution — "Morwen's hand near Cleaver's hilt out of old habit" (field, t1833).** TLDR: the player told two companions to sleep at the inn, the engine never re-split them, and when asked where they were the GM confabulated their presence — decorating it with the PLAYER's sword in a non-sword-user's hand. Evidence (t1833 save): t1826 Daeris+Morwen split to the Shore District ([PARTY_SPLIT] live); t1827 player: "You two get some sleep, Friz and I can deliver it"; t1828 they return to the inn — CO-LOCATED, so the #133b fold correctly rejoins them (muts: "Daeris rejoins", "Morwen rejoins") and Morwen is narrated to bed; t1829 Ammut+Frizwick leave for the Justice Court with NO new [PARTY_SPLIT:] — the roster now says all four present; t1832 player asks OOC "GM: Where are Daeris and Morwen?"; t1833 the GM answers from its (wrong) roster: "still with you… followed straight from breakfast", plus "Morwen's hand rests near Cleaver's hilt out of old habit" — Cleaver lives ONLY in Ammut's inventory, and Morwen's sheet carries NO sword at all (throwing knives "for contingency not preference"). State stayed clean: t1833 emitted zero mutation tags, and the OOC exchange is RAG-excluded by the meta-exchange guard, so the lie cannot poison retrieval — the damage is prose-momentum only. **Two defect classes, design-first (split machinery + any narration guard = drift surface):** ⓐ the split machinery is edge-triggered by GM tags, so a re-departure declared in the PLAYER's OWN INPUT ("you two get some sleep") has no engine path — candidate: a #168-W4-style stay-behind observer (player-input subgroup-departure shapes arm a one-shot GM-decides [PARTY_SPLIT:] nudge; prose never moves anyone); ⓑ prose item-owner binding has no guard anywhere — W6 validates pronoun subjects in summaries only — candidate: a narrow committed-prose scan for "<companion>'s <distinctive item name>" where the item resolves uniquely to ANOTHER character's sheet via itemBaseName, arming a one-shot correction nudge (never rewrites; the #158 clockPhaseAssertion precision discipline applies). Secondary check while in there: whether the t1828 fold should have routed to buildSplitAudit under #164's granularity rule (splitLoc "Magnimar (Shore District)" folded against bare Magnimar, a node with known interiors) — right outcome this time, possibly by luck. Save: `testRuns/Rise_of_the_Runelords__Ammut__Ammut_t1833.tnd`.

## Status / record

Filed 2026-08-14 from the owner's live report — investigation only, both mechanisms verified in the save; fix is design-first. **t1834 follow-up CONFIRMED the state gap live:** the GM's prose correction ("corridor holds only you and Frizwick… still abed at the inn") emitted NO [PARTY_SPLIT:] — and the #133c "(split: …)" HUD marker's absence proved it at a glance (the away-marker already existed and told the truth; owner ruling: transitions must be LOUD too). **Hardened v1.625 (same day):** all three transition sites toast — ⇢ on split-set, ⇠ on explicit |rejoin, ⇠ on the #133b auto-fold (the transition most likely to pass unnoticed) — engine-tested through the real handler (1410 green). **v1.628 (owner field report, same day): the split toast is TRANSITION-GATED** — the GM legally re-affirms an active split every turn (refreshing the #133 audit clock), and v1.625 toasted every re-affirm (t1835–1837: a toast pair per turn, no oscillation — state was verified rock-solid, splitLoc unbroken, zero folds); now only the not-split→split edge or a changed away-location toasts, sabotage-proven (gate removed → test red). ⓐ stay-behind observer + ⓑ item-attribution nudge remain design-first. **ⓒ camera-omission split misuse (field, t2095–2097, grounded in the t2097 save):** the GM forgot Morwen in its own forge scene for two responses, papered over the omission by emitting `[PARTY_SPLIT:Morwen Zethran|Magnimar (Wyla Ashvane's shop)]` — splitting her TO THE SHOP THE PARTY WAS STANDING IN — then, asked "where did Morwen go?", narrated "Morwen never left the shop at all… stationed near the front window" while RE-AFFIRMING the split in the same response (prose contradicting its own tag); self-resolved via rejoin at t2097, canon cost ≈ zero, and the v1.625 toast + #133c HUD marker are exactly how the owner caught it. The engine held the split defensibly (#164 granularity: party at bare Magnimar vs split to its child — parent-vs-child never folds). Design candidates for the same pass: (a) refuse/convert a split whose target locSame/locIsSub the party's current position ("a split to where the party stands is not a split; an out-of-frame character needs no tag") — risks blocking legitimate pre-staging before a departure; or (b) allow but arm a one-shot audit that nudges a fold if the party is still co-located at the parent after N turns — never blocks, catches the misuse. (b) favored. ⓐⓑ filed 2026-08-14; ⓒ added 2026-08-20 from the t2097 trace. ✅ **ⓒ SHIPPED v1.687 (2026-08-21, lane A) — as an IDENTITY fix, not a timer:** investigation found `buildSplitAudit` ALREADY waives its age gate for same-world splits (the (b) behavior ships since #133b) — t2097 escaped it only because the GM wrote the interior PARENTHETICALLY ("Magnimar (Wyla Ashvane's shop)"), a distinct name-born key `locSame` can't see through. The PARTY_SPLIT write boundary now normalizes "X (Y)" → location X | sublocation Y when the OUTER resolves to a KNOWN world node and no explicit sublocation was given (unknown outers never guessed; 3-field form untouched — both pinned); the normalized shape makes the existing same-world waiver audit the split IMMEDIATELY (test-proven on the exact t2097 tag). 4 failing-first tests (1579 green); diff-replay 4/4 corpora byte-identical. ✅ **ⓐⓑ SHIPPED v1.688 (2026-08-21, lane A) — ROW CLOSED.** ⓐ: `detectPlayerStayBehind` (helpers.js) — the PLAYER-INPUT twin of #137's GM-prose watcher: named stay-directives ("Morwen, stay here"), nameless subgroup directives ("you two get some sleep" — the exact t1827 sentence, test-pinned; the GM resolves WHO), and exclusive continuations ("Friz and I can deliver it"); rejection-first (questions, negations, accompany-forms "stay close/with me", whole-party rest, player-self); armed at the commitGmTurn seam only when the GM response carries no [PARTY_SPLIT:], delivered by `buildPlayerSplitNudge` (one-shot, 2-turn shelf, GM-decides — prose never moves anyone). ⓑ: `detectItemMisattribution` (helpers.js) — a DISTINCTIVE item (capitalized-as-stored base name on exactly ONE party sheet) attributed to a different member by direct genitive OR possession-by-handling ("Morwen's hand … Cleaver", the t1833 shape, test-pinned); case-sensitive item match, quoted sentences skip, owner-named-in-sentence skips; `buildItemMisNudge` teaches transfer-or-let-it-return, never rewrites. Both pings ride NOTE_LATCH_FIELDS. 10 failing-first assertions incl. both precision batteries (1589 green).
