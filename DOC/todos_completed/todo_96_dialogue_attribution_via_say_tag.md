# TODO #96 — Dialogue attribution via `[SAY:]` tags — the GM names each speaker at authoring time (replaces the #9 LLM speaker post-pass).

*Full record extracted verbatim from TODO.md (Feature backlog, completed row) on 2026-09-01; the tracker row keeps the TLDR and the verdict. Table pipe escapes (`\|`) are unescaped here; `<br>` breaks became paragraphs.*

**Effort:** M · **Tier:** Fable

## Task

**Dialogue attribution via `[SAY:]` tags — the GM names each speaker at authoring time (replaces the #9 LLM speaker post-pass).** TLDR: voices were misattributing in the field (user screenshots t1167/t1168, 2026-07-26 — a line WITH explicit attribution still misbound) because the GM's knowledge of who speaks was thrown away and re-derived by a second model call guessing over the finished prose. Now the GM emits `[SAY:Character Name]` immediately before every quoted line (one STATE TAGS doc line; tag_table entry — parse-less like DICE/RETCON, stripped from display/transcript everywhere; the `|descriptor` payload is RESERVED for delivery styles — whispers/shouts — later). **`deriveSpeakerMapFromTags(raw,clean)`** (game.js, pure): each tag captures its following quote's opening in the RAW response and binds the matching dialogue span in the CLEAN text (normalized prefix, in-order consumption so identical lines bind one-to-one); everything untrustworthy DROPS to narrator, never guesses; unknown names defer to `speakerVoiceMap`'s existing speak-time resolution (where #95.7 auto-cast also still applies). Storage/replay UNCHANGED (`{n,s}` shape, sp.n splitter fuse, live re-resolve). **DELETED:** assignSpeakers/buildSpeakerPrompt/parseSpeakerMap/speakerCastList/speakerPassNeeded, the per-turn 400-token "speakers" call, its 4s wait fuse, and the 60-unit cap. **Bonus:** muted turns now keep their maps (derivation is free — the old pass skipped muted turns to save the call, leaving their replays voiceless forever). Cost: ~10 output tok per spoken line vs the deleted call (~1k in + ≤400 out per dialogue turn) — net saving AND narration no longer waits on a round trip. Immediate cutover per user (no post-pass fallback: an untagged line narrates). ⛨ Drift surface — strip + doc hashes re-baselined deliberately (CT_TAGS 1645986938/989, doc 1259906480/16295).

## Status / record

✅ **SHIPPED v1.447 (2026-07-26, built by Fable; design + immediate-cutover ratified by user same day).** 834 green: 4 new derive tests (span expansion + no-bleed, in-order duplicate binding, PC-binds-by-NAME with reserved descriptor — the B14d concern made structural, drop-never-guess battery) — sabotage-verified (removed the in-order consumption → test tripped); dead post-pass tests deleted with tombstones; the #16 TTS-hint test now exercises the real producer. ⚠ **FIELD WATCH (next live session):** ① prose-voice fidelity under the new tag duty — the #14 [ACTIONS:] lesson says an in-prose format duty can tax the voice; if prose feels off, suspect this first; ② ~~real-GM tag emission rate~~ **② ANSWERED same night — emission was ZERO** (user report "not working, everything"; server session_log evidence: 3/3 recent responses dialogue-heavy, no [SAY:] anywhere — the sessionLog full of the GM's own untagged prose out-momentumed the doc line, the EXACT prompt-channel-beats-position class). **Fix v1.449: `buildSayComplianceNudge`** (api.js, NOTE_BUILDERS entry — the channel that beats momentum): fires while the newest GM response has quoted dialogue and no [SAY:], silent the moment compliance starts (permanent-noise guard engine-tested); 835 green; ③ non-Claude providers: SAY is deliberately NOT in TAG_REINFORCE yet — add it there if a money-test shows gpt/grok/gemini skipping it. 



**v1.451 (same night) — the deriver reworked on field evidence ("1 of 4 bits assigned"):** the nudge WORKED (5 tags in the live t1170 response — pulled from the server and replayed through the real pipeline) but v1's first-quote-per-tag matching voiced only 3 of 9 spans: real speeches are MULTI-SPAN ('"Steady," she says. "First time…"' — continuations narrated flat), and the #93 adjacent-paragraph span MERGE glued two speakers into one span, silencing the second tag and breaking prefix matching. **Rework: unit-level SEGMENT claiming** — the raw text is cut into segments at the [SAY:] tags (each tag owns everything until the next), and each dialogue unit is located in the segments with a forward-only cursor, taking its segment's speaker. Continuations inherit their tag; a merged span's units land in different segments and SPLIT correctly (the #93 edge neutralized for voicing). Pre-first-tag text and unlocatable units stay narrator — still drop-never-guess. Verified: the real field response goes 3→18 voiced units, every line correct incl. both merged spans; condensed field fixture added as an engine test (836 green, sabotage-verified with precise diagnostics). 



**v1.452 — non-party NPCs can be cast (the "Ameiko speaks with a voice, Hemlock gets the narrator" field question):** voice resolution needs a charSheet with gender, and the UI had NO path to one for a roster NPC — the sidebar's non-party rows weren't clickable, `showNpcSheet` displayed a sheet only for party members, and the Generate button was `isParty`-gated. Three ungates: sidebar non-party rows → `showNpcSheet` (same data-npc pattern as party rows); the sheet renders for ANY NPC that has one (voice control included); Generate offered to any roster NPC (party-only features — portrait edit, Part ways, Play-as, Export — keep their gates). Generator prompt de-adventurered ("townsperson, official, merchant… choose class and stats that fit who they actually are"). Flow: sidebar → Hemlock → Generate → sheet lands with gender → auto-cast picks a male bench voice next turn. 



**#96b (v1.453, user call: "add a voice field so Ameiko doesn't sound different every time"):** the auto-cast pick is PINNED to `charSheet.voiceId` — the hash was stable turn-to-turn, but a bench edit re-dealt every unpinned character. Two stamp points: ① **first speech** — `pinAutoCastVoices(sp)` (game.js, called beside the map stamp in narrateWithSpeakers): each mapped name resolving to a sheet with no voiceId gets the auto-cast pick written permanently (console.info per pin; assigned voices untouched; sheet-less/NB names skip — never guess); ② **sheet creation** — generateNpcSheet stamps a pick on a fresh sheet (regenerates carry the prior pin, unchanged). Pins ride the sync blob and show in the sheet's voice dropdown for re-pinning. Also fixed riding along: generateNpcSheet hardcoded `sheet.partyMember=true` — correct when only companions could generate, WRONG since v1.452 opened generation to roster NPCs (now `!!wsNpc.partyMember`; the only reader keys off the roster flag, verified). 837 green incl. the no-overwrite/no-repin/NB-decline negatives; sabotage-verified. 



**ROW CLOSED 2026-07-27 (audit):** shipped v1.447 and hardened through the same night's field loop — v1.449 compliance nudge (GM emitted zero tags; engine-note channel fixed it), v1.451 segment-claiming rework (1-of-4 voiced → all lines, proven on the real turn), v1.452 roster-NPC sheet access, v1.453 voice pinning. **Watches that survive the close:** ① prose-voice fidelity under the tag duty (the #14 class — if prose feels off, suspect this first); ② SAY is deliberately NOT in TAG_REINFORCE — add it if a money-test shows non-Claude providers skipping it. #93 (rescoped) owns the surviving splitter edge (unclosed-quote parity → narration voiced as a character).
