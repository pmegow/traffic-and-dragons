# TODO #144 — DRIFT HARDENING: NPC knowledge lifecycle — archive every eviction, converge over-caps, keep newest on render; then type the facts.

*Full record extracted verbatim from TODO.md (Feature backlog, completed row) on 2026-09-01; the tracker row keeps the TLDR and the verdict. Table pipe escapes (`\|`) are unescaped here; `<br>` breaks became paragraphs.*

**Effort:** M · **Tier:** Fable

## Task

**DRIFT HARDENING: NPC knowledge lifecycle — archive every eviction, converge over-caps, keep newest on render; then type the facts.** TLDR: NPC knowledge is the only memory tier that silently destroys canon — evictions, merges, truncation; archive every loss, then split durable canon from scene chatter. 



**Solution — Phase A (mechanical; the bleeding stops):** ① both write-site caps (memory.js:976 summarize path, :940 supersede path) stop `.shift()`-ing to the void — evicted fact → `memArchive().npcKnowledge.push({npc,fact,turn})`, with `npcKnowledge` added to the memArchive/healMemory self-heal set; ② the `[NPC_MERGE:]` re-slice (tag_table.js:318) archives its truncated overflow + loud console.warn + a muts line ("merge truncated N facts — archived"); ③ `fileNpcEvent`'s events cap-8 slice archives the same way; ④ load-time convergence in `migrateWorldState`: knowledge length>12 → archive the OLDEST overflow, keep newest 12, one console.info per NPC (heals Frizwick 18→12 with 6 facts archived, not destroyed); ⑤ the render truncation (`memoryNpcDetail`, the 2000-char cut) flips to keep the NEWEST whole facts, prefixed "(N older facts omitted — archived)"; ⑥ the ui-files.js import whitelist gains `archive.npcKnowledge` AND the already-dropped `archive.superseded` (Sol's find — imports silently lose it today). Red-first per clause; the t1549 fixture proves Frizwick converges and Morwen's carved-entities fact would have survived. **Phase B (design, DRIFT PASS order 8, after A ships):** extractor schema `knowledgeGained` → `{fact, kind:"durable"/"scene"}` — scene-kind routes to turn-stamped events, never standing Knows; `memoryNpcDetail` gains a conservative conflict gate (suppress position claims contradicting `splitLoc`/current node). Model-facing extractor change → money-turn validation (the gpt-4o precedent); Phase A's archive is the safety net for any misclassification. 



**Evidence (audit round, both reports):** Fable's longitudinal diff — 83 knowledge facts destroyed t1265→t1549 (Sol's exact-text variant counts 115), including Morwen's active-arc carved-entities fact, while lore/decisions/chapters lost ZERO (their P12 archive discipline works); the #128 merges cut Morwen's ~36-fact three-key history to exactly 12, archive-less. Sol's prompt reconstruction — the extractor schema offers one untyped `knowledgeGained` string; `applySummaryExtract` stores it permanently, and `memoryNpcDetail` labels the whole array `Knows:` with no turn, validity, or fact kind. The latest t1549 save contains direct contradictions: Morwen is currently with Ammut en route to Savah, but her injected detail says she “is now stationed outside the sealed door” and “remained outside”; Daeris is split at the ironworks but her detail says “Present at the carved door” and “Arrived at the workshop”; Frizwick is split at the ironworks but her detail says she “is holding the chokepoint”. Frizwick also has **18 knowledge entries despite the cap of 12**; the write boundary sheds only one entry per write, so a legacy overfill stays overfilled, while the 2,000-character renderer truncates from the tail and therefore preserves the oldest/stalest claims while hiding newer facts. Reconstructing the real t1549 prompt proves these lines are actually served, not merely stored. Counterweight: `splitLoc`, `lastSeenAt`, and the split geography block are currently correct and mitigate the conflict; identity fragmentation did not reproduce. Candidate direction: typed/temporal memory facts, scene-state facts routed to events/RAG rather than standing knowledge, load-time cap convergence, and render-time structured-state conflict filtering.

## Status / record

✅ **Phase A SHIPPED v1.564** (2026-08-08, drift pass order 1): all six clauses landed — both write-site caps + the merge re-slice + fileNpcEvent evict to `memory.archive.npcKnowledge`/`npcEvents` (merge warns + muts line); healMemory converges over-cap lists (oldest→archive, newest 12 kept, idempotent — proven on the REAL t1549 save: Frizwick 18→12, 6 archived intact); memoryNpcDetail sheds OLDEST whole facts under the 2000-char budget with a "(N older facts not shown)" marker (P8 hard-cut kept as backstop; row text said "— archived" but render-only truncation doesn't archive — the facts stay live, deliberate wording fix); the import whitelist carries the FULL archive incl. the pre-existing superseded/coreMemories/expiredSchedules drops. 6 red-first tests → green (1088); #144A ARCHIVE CARRY CONTRACT (run-tests.js) pins the whitelist + no-bare-shift, whitelist clause sabotage-proven by name, write sites guarded by the behavioral tests (a grep clause can be evaded by variable aliasing — the engine tests are the load-bearing guard there, proven tripping). ✅ **Phase B SHIPPED v1.572** (2026-08-08, drift pass order 8): the extraction schema now teaches `knowledgeGained` as `{fact, kind}` — durable (standing truth) vs scene (true only in the moment); apply-side routes scene facts through `fileNpcEvent` as turn-stamped history, never standing Knows; unknown kind defaults durable (safe now that #144A archives evictions); the legacy string shape stays byte-compatible, so a model ignoring the new schema degrades to today's behavior, never breaks. The conflict gate shipped as the ZERO-false-positive form: `memoryNpcDetail` leads a split party member's block with an authoritative "Currently: AWAY from the party at X — claims below that contradict this are STALE" line — pure addition, no suppression (a misclassifying suppressor would hide TRUE canon, the rebuttal-round objection). 3 red-first tests → green (1118); schema-teach clause pinned in the #144A contract. ✅ **Money-turn VALIDATED 2026-08-09** (live probe on the player key, rider on the #148 gate run — [audits/AUDIT_recall_gate_v1.573.md](audits/AUDIT_recall_gate_v1.573.md)): the model emitted exactly the typed shape with correct classification on the first try — Wyla's watcher arrangement filed `durable`, her sealing-a-box moment and Morwen's in-progress errand filed `scene`. The probe's own parse hiccup was an instrument artifact (mini-prompt lacked the real pipeline's B11 note-stripping + anti-markdown enforcement; summarize() carries both + the 3-strike retry). Field watch: the first real summarize on v1.572+ should show scene facts landing as dated history, not Knows growth.
